select ?name ?abstract where{
GRAPH <{{lodspk.graph}}>{
    ?schema a owl:Ontology;
            rdfs:label ?name;
            dcterms:abstract ?abstract .
    FILTER(lang(?name) = "en")
    FILTER(lang(?abstract) = "en")
  }
}
